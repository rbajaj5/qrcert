[package]
name = "qrcert-checker"
version = "0.1.0"
edition = "2024"
publish = false
description = "Extraction-oriented checked decoder for the QRCert fixed-byte blueprint"

[lib]
path = "src/lib.rs"

[dependencies]

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = "deny"
pedantic = "deny"
